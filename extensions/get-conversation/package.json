{
  "name": "get-conversation",
  "version": "4.0.0",
  "description": "Obtains a Cognigy conversation transcript, via OData",
  "main": "build/module.js",
  "scripts": {
    "transpile": "tsc -p .",
    "zip": "tar cfz ${npm_package_name}-${npm_package_version}.tar.gz build/* package.json package-lock.json README.md icon.png",
    "build": "npm run transpile && npm run lint && npm run zip",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "clean": "rm -rf build node_modules *.gz"
  },
  "author": "Cognigy GmbH",
  "license": "MIT",
  "dependencies": {
    "@cognigy/extension-tools": "^0.10.0",
    "axios": "^0.20.0",
    "juice": "^7.0.0",
    "moment": "^2.27.0"
  },
  "devDependencies": {
    "@types/node": "^13.13.15",
    "tslint": "^6.1.2",
    "typescript": "^3.8.3"
  }
}