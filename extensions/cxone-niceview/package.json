{
  "name": "niceview",
  "version": "1.0.3",
  "description": "NiCEview extension for applying NiCEview demo settings in your Cognigy flows",
  "main": "build/module.js",
  "scripts": {
    "secrets": "node ensure-secrets.js",
    "transpile": "npm run secrets && tsc -p .",
    "zip": "node zip.js",
    "build": "npm run transpile && npm run lint && npm run zip",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "test": "npm run transpile && node --test test/",
    "verify": "npm run transpile && npm run lint && node --test test/"
  },
  "keywords": [
    "NiCEview",
    "NiCEview Context",
    "NiCEview Settings"
  ],
  "author": "NiCE Alexei",
  "license": "NiCE",
  "dependencies": {
    "@cognigy/extension-tools": "^0.14.0"
  },
  "devDependencies": {
    "tslint": "^6.1.3",
    "typescript": "^5.9.2"
  }
}
